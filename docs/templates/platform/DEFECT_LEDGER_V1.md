# Template Platform v1 defect ledger

Status: Candidate / Canonical=false  
Integration base: `df84e872f987edf07c8dabade6adac4613ca9182`  
Frozen contract: `1bce8f9e0312df9a29635a6988b62cd297b1ab14`

This is the release-blocking defect ledger required by the Final Production
4-Conversation Pack. A green test suite is supporting evidence, not a release
decision. Every implementation candidate must be reviewed at an exact commit
or staged-diff hash by someone who did not author that candidate. A fix always
invalidates the previous review and requires a new independent review.

## Open internal defects

| ID | Severity | Lane / reviewed object | Finding | Required closure evidence | State |
| --- | --- | --- | --- | --- | --- |
| C0-001 | P1 | C0 staged diff `996e37f6237c56173ef670c3485ca7aa833bdf55bc641c80aca9c497a6e8754f` | Lifecycle and migration receipts could be replayed after persisted binding fields were modified. | Canonical receipt verification and tamper tests for lifecycle, migration, restore, runtime and revision/order fields. | Fixed in frozen candidate; independent review pending |
| C0-002 | P1 | same | Runtime output, Core approval consumption, production signature/provider wiring, outbox recovery and governed UI actions were incomplete. | Atomic Core consumption, existing Artifact readback, leased outbox recovery, Ed25519 verifier, real provider wiring, full API/CLI/UI tests. | Fixed in frozen candidate; independent review pending |
| C0-003 | P1 | current dirty C0 fix | Shared tests exposed missing `re` import and post-upgrade migration-plan readback rejected `target == installed`. | Both Python versions pass the complete shared suite, including regression assertions. | Fixed in frozen candidate; independent review pending |
| C0-004 | P1 | C0 pre-freeze staged candidate | The openJiuwen lock called annotated tag object `3a322735...` the source commit, while `v0.1.16` peels to commit `1bd42cfe...`; source provenance and SBOM were semantically wrong. | Lock, runtime health, documentation and SBOM distinguish exact tag object from peeled commit; deterministic tests bind both identities. | Fixed in `aa667fcc012a5eed4a6e823741d8867f750417a1`; independent review running |
| C0-005 | P1 | C0 commit `aa667fcc012a5eed4a6e823741d8867f750417a1` | `resume` calls the external provider before committing PreparedAction consumption and the durable resume intent. A provider-success/local-receipt-failure rollback leaves the action approved and a retry calls the provider a second time. | Commit an immutable resume intent and consumed Core authority before the external call; after any ambiguous outcome retry/restart reconciles authoritatively and never launches a second resume. Include provider-success/receipt-failure/reopen fault injection proving one provider call. | Independent review failed; fixing |
| C0-006 | P2 | same | The visible UI cancel operation posts an empty body, while the backend requires the exact canonical SHA-256 action hash. | UI obtains or computes the same canonical cancel action hash as the backend; handler/UI tests exercise the real request. | Independent review failed; fixing |
| C0-007 | P2 | same | The tracked SBOM truthfully remains a candidate artifact with a source-commit sentinel, but there is no reproducible external release-candidate SBOM bound to the exact reviewed commit. | Deterministic generator accepts and validates an exact 40-hex source commit and emits an external exact-candidate SBOM without rewriting the tracked candidate artifact. | Independent review failed; fixing |
| C0-008 | P1 | C0 corrective commit `7691d4a38613e23f42f9a433ddc2cffe188ba365` | A concurrent retry can reconcile and finalize the durable resume intent while the original provider call is still in flight. When the original returns, its failed finalize is caught by the broad exception path and appends `UNCERTAIN`, overwriting the already authoritative reconciled state. Exact repro produced `resume_calls=1`, `reconcile_calls=1`, a finalized intent, then request/latest receipt `uncertain`. | Intent ownership/CAS and terminal-state protection prevent late provider or failure paths from overwriting an authoritative finalized reconcile. Add concurrent original-call/retry/reconcile/cancel tests and prove deterministic convergence/replay. | Fresh independent review failed; fixing |
| C1-001 | P1 | C1 commit `b6fb216661215eb524c1ed3ff1c24453c474f46e` | Real C0 `TemplateSDK` rejects Core references registered as domain repositories, undeclared per-route API registrations and a memory policy without a namespaced ID. | Mount with the real C0 SDK; exact declaration counts; no MIS Core duplication; all entrypoints callable. | Fixed in `343cae665e2634d5a990920e8cffa9595cbac152`; independent review running |
| C1-002 | P1 | same | Domain code holds an HMAC signing secret and can forge an `allow` Core receipt. | C0 public-key-only, revocable, purpose-scoped receipt verifier wired through the production composition root; signing helpers restricted to tests. | Fixed in `343cae665e2634d5a990920e8cffa9595cbac152`; independent review running |
| C1-003 | P1 | same | SSH process launch occurs before a durable launch fence; a crash can relaunch the same attempt. Marker writes also lack file/directory fsync. | Fence-before-launch protocol, authoritative restart reconciliation and crash-injection tests at each persistence boundary. | Fixed in `343cae665e2634d5a990920e8cffa9595cbac152`; independent review running |
| C1-004 | P1 | C1 commit `343cae665e2634d5a990920e8cffa9595cbac152` | The C0 dependency test reads a mutable sibling tree and pins only the historical contract commit, so the SDK/trust dependency is not reproducible. | Export exact C0 implementation commit `aa667fcc...` into an isolated tree; reject the historical contract-only tree and unexpected C0 worktree/index drift. | Independent review failed; fixing |
| C1-005 | P1 | same | Registration helpers exist, but the production composition does not construct the concrete C0 verifier and mount the Research entrypoints through C0's startup allowlist. | A production composition root owns the exact C0 verifier and `TemplateEntrypointRegistry`, mounts the declared entrypoints and proves live callable handlers. | Independent review failed; fixing |
| C1-006 | P1 | same | SSH reconciliation accepts injected `authority` / `authoritative` booleans instead of a purpose-scoped, public-key-verified remote registry receipt. | Reconciliation verifies an exact attempt/request/PID/start-identity receipt with the concrete C0 verifier; missing/forged/ambiguous evidence remains `remote_unknown` and cannot relaunch. | Independent review failed; fixing |
| C1-007 | P1 | same | The repository secret smoke script contains two literal forbidden live-key patterns and fails against the exact candidate. | Construct negative fixtures without committing a detectable credential-shaped literal; exact repository scan returns zero findings. | Independent review failed; fixing |
| C1-008 | P2 | same | Handoff metadata reports stale commit/test counts. | Regenerate exact handoff metadata from the new candidate and actual gate output. | Independent review failed; fixing |
| C2-001 | P1 | C2 commit `2eb0d6699aedb83ab8fb67658aebdbaf2ed3b422` | `OfficialCareerSimulatorAdapter` accepts caller-controlled module metadata booleans as proof of an official distribution; no public-key verifier is injected. | Use the C0 public-key-only verifier for the exact distribution/version/bridge contract and receipts; ordinary callers cannot construct an official composition root. | Independent review failed; fixing |
| C2-002 | P1 | same | The governed service has official report/export methods, but the manifest exporter is the public fail-closed exporter and no official report/export operation is registered in API, CLI or UI. | Exact Core-terminal-bound report and submission operations exposed through authenticated API/CLI/UI, with live handler and UI tests. | Independent review failed; fixing |
| C2-003 | P1 | same | The SDK mount test imports an uncommitted `sdk.py` from a mutable sibling C0 worktree; neither reviewed Git tree contains that object. | Mount against a frozen reviewed C0 commit/tree and record its exact hash. | Independent review failed; fixing |
| C2-004 | P1 | same | Migration SQL execution failure can commit partial `ALTER TABLE` changes while leaving the old schema version; only post-readback failure restores backup. | One transaction or authoritative backup restore for every execution exception; fault injection proves schema and version remain consistent. | Independent review failed; fixing |
| C2-005 | P1 | same | Manifest provenance points to intermediate `b7fefbee...`, not the exact reviewed implementation. | Governed packaging stamps and verifies the exact source commit; candidate documentation remains truthful before stamping. | Independent review failed; fixing |
| C2-006 | P2 | same | Scope test uses an intermediate base and handoff claims JSON Schema verification when the dependency was skipped. | Use frozen contract `1bce8f9e...` and run the schema test in the available `jsonschema` environment before updating handoff. | Independent review failed; fixing |
| C2-007 | P1 | C2 commit `80b2a2dac8299590baab3aff76e3bf099f009d02` | Official monthly receipts, terminal acceptance/export and runtime receipts trust caller-supplied `verified` / `signature_verified` / `trust_status` booleans plus self-computable binding hashes. A forged structural Core port can mint an official submission without a private key. Lifecycle authorization uses the same trust anti-pattern. | Production service/report/lifecycle composition owns the exact C0 operator-configured public-key verifier. Core returns opaque payload/proof envelopes; each purpose and complete binding is cryptographically verified with revocation. Add forged-callback/boolean negative tests and real Ed25519 positive tests against exact C0. | Independent review failed; fixing |
| C3-001 | P1 | C3 commit `8db69ea12293d40590323c26002865eca4ad94e3` | A Risk Officer evaluation can be reused to execute a different portfolio proposal. | Canonical proposal hash bound through RiskDecision, Core Evaluation, checkpoint, protocol, backtest, artifacts, report, audit and final receipt. | Fixing |
| C3-002 | P1 | same | RiskDecision is not durably stored/read back; database replacement can leave one proposal ID with divergent durable and returned content while restart completes. | Immutable persisted/read-back RiskDecision and approved proposal; body/hash replacement and restart attacks fail closed. | Fixing |
| C3-003 | P2 | same | The exact-approved-weights test and handoff claimed coverage that did not exercise post-approval replacement. | Correct adversarial tests and truthful handoff. | Fixing |
| C3-004 | P1 | C3 fix candidate `cd77b3ba2355c19fb8bf87e1fdf6ceaf99cc6d10` | Restart only compares project/run, so the same workflow/run can resume with a different request and Core action/PreparedAction/Approval tuple after `risk_passed`. | Persist and revalidate the exact request hash and complete Core authority tuple on every restart before any read or side effect. | Fixed in `3361ed4063208e0044cdf621db45884a017e9e38`; independent review running |
| C3-005 | P1 | same | Rewriting Proposal, RiskDecision evaluation binding and checkpoint together can pass restart because the approved execution hash omits the evaluation binding and readback does not bind proposal hash metadata. | Immutable Core Evaluation binding to proposal hash and decision identity; checkpoint rewrite cannot substitute it; deterministic decision ID is recomputed. | Fixed in `3361ed4063208e0044cdf621db45884a017e9e38`; independent review running |
| C3-006 | P2 | same | A copied RiskDecision with an arbitrary new ID is accepted after the checkpoint points at it. The line's SDK test also uses a conformance double rather than the real C0 class. | Recompute deterministic decision identity and run a subprocess against the real C0 `TemplateSDK`. | Fixed in `3361ed4063208e0044cdf621db45884a017e9e38`; independent review running |
| C3-007 | P1 | C3 commit `3361ed4063208e0044cdf621db45884a017e9e38` | The claimed exact C0 SDK mount referenced historical contract commit `1bce8f9e...`, which contains no `template_runtime/sdk.py`, while tests imported a mutable sibling worktree. | Export exact C0 implementation commit `aa667fcc...` with `git archive`, prove the historical commit fails, and preserve/validate the known C0 dirty-file allowlist. | Fixed in `b3828a2e0eb933d5211737a53f4b8cfca3e750d5`; fresh independent review running |
| C3-008 | P2 | same | Handoff branch/fix metadata was stale and not reproducible. | Record the actual branch, parent and runtime-derived exact candidate strategy. | Fixed in `b3828a2e0eb933d5211737a53f4b8cfca3e750d5`; fresh independent review running |
| C3-009 | P1 | C3 commit `b3828a2e0eb933d5211737a53f4b8cfca3e750d5` | Governance, runtime, evidence, memory and outbox ports return naked mappings whose predictable `verified`, `allow` and `signature_verified` fields are trusted without a C0 cryptographic receipt. Forged ports with no private key complete the workflow and mint a trusted-looking QuantReceipt, Claim Gate, artifacts, evaluations, audit and memory. | Production composition owns exact C0 `TrustedCoreReceiptVerifier`; every Core authority readback is a purpose-separated payload/proof envelope with complete bindings and revocation checks. A forged full-chain port must fail before any trusted receipt or claim is emitted. | Independent review failed; fixing |
| C3-010 | P2 | same | Handoff records the literal placeholder `runtime_derived_by_git_rev_parse_head` rather than an immutable exact candidate identity or truthful external-stamping strategy. | Candidate handoff remains explicitly unstamped, and a strict external release artifact binds the reviewed 40-hex commit; never claim a runtime placeholder is the frozen SHA. | Independent review failed; fixing |
| INT-001 | P1 | integration head after C1 merge | Research domain adapters are merged, but the shared product has no concrete C0 implementations of `ResearchCorePort`, `GovernedResearchOperationsPort`, migration/runtime/evidence ports or a Core receipt-signing composition. `server.py` has no template-domain record transaction authority, so mounting `ResearchAPI` now would be a false-success shell. | Implement Core-owned domain tables/transactions/outbox, purpose-separated receipt issuance with operator-owned signing material, concrete Research adapters, authenticated `CoreRefs` construction and exact readback/tamper/revocation tests. | Open on combined head |
| INT-002 | P1 | same | The 41 Research routes, React extension and `agentops template research-lab` CLI are not mounted in the real shared server/AppShell/CLI. | After INT-001, mount all exact declarations through C0 Registry; add authenticated server integration, AppShell deep-link/browser and shipped CLI live delegation tests. Partial registration must fail activation. | Open on combined head |

## Pending independent decisions

| Lane | Exact candidate | Required reviewer state |
| --- | --- | --- |
| C0 Shared Runtime | `7691d4a38613e23f42f9a433ddc2cffe188ba365` | Corrective commit closes prior resume/UI/SBOM findings and passes author gates, but fresh review reproduces C0-008 concurrent convergence failure. Exact decision remains FAIL; another fix and non-author review are required. |
| C1 Research | candidate `a9fa0f02deeab9b3f0f0c90ea4edcd314e644bd0`; integration `5cdc9570c31ef821f12cafb0e17b399a939a72e8` | Fresh exact independent PASS: P0=0, P1=0, P2=0. Merged without conflict. Combined head reran Python 3.11/3.14 90/90, secret scan 1010 files/0, compile/diff/clean tree and isolated esbuild 0.25.12 bundle. Shared server/AppShell/CLI live wiring remains a truthful combined-head gate. |
| C2 Career | `80b2a2dac8299590baab3aff76e3bf099f009d02` | Corrective candidate passes its author suites, but independent trust-boundary review reproduces a forged-Core official submission. Exact decision remains FAIL pending C2-007 correction and fresh review. |
| C3 Quant | `b3828a2e0eb933d5211737a53f4b8cfca3e750d5` | Fresh exact review FAIL: P0=0, P1=1, P2=1. Core trust-envelope and exact-handoff corrective implementation is running; any new commit requires another non-author review. |

## External gates (isolated; not internal defect waivers)

| Gate | Current evidence | State |
| --- | --- | --- |
| Research real GPU / SSH / Slurm | No governed target credentials or real scheduler allocation are available. Fixtures cannot satisfy the production gate. | `BLOCKED_EXTERNALLY` |
| Career official 48-month simulator | The official simulator distribution and exact version are not available. Reference runs remain non-official. | `BLOCKED_EXTERNALLY` |
| Quant official market data | No governed official data entitlement/receipt is available. Reference snapshots remain non-official. | `BLOCKED_EXTERNALLY` |
| Governed deployment | Deployment authentication and an approved release action are not available in this execution context. | `BLOCKED_EXTERNALLY` |

External gates do not stop safe implementation, review or integration work.
They do prevent `FINAL_STATE: COMPLETE`, canonical promotion, release claims and
production deployment until execution plus authoritative readback receipts exist.

## Release rule

Integration is allowed only for an exact candidate with no open P0/P1/P2 in its
independent review. Final release additionally requires zero global P0/P1/P2,
all exact-integration regression gates, MIS dogfood readback, branch protection,
governed external writes and a verified final release receipt.
