# Common Failures

- Agent starts coding without reading the project spec.
- Agent ignores existing base constraints and duplicates a runtime feature.
- Agent records raw prompt or response text instead of a safe summary and hash.
- Agent proposes memory as authority without review.
- Live runtime work is attempted without explicit confirmation.
- A UI route is used as an agent execution path instead of CLI/API.
