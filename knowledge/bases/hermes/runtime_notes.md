# Hermes Runtime Notes

- Local gateway is normally `http://127.0.0.1:8642`.
- Confirmed live runs should write MIS evidence.
- If the gateway is unavailable, record a blocked/failed runtime event instead of hiding the failure.
