# Full Research Lab profile

This profile enables the complete production surface. SSH GPU and openJiuwen
must return real receipts before release acceptance. Slurm is required whenever
the selected research infrastructure uses Slurm; an approved ADR may exclude
it only after the actual target boundary is known.
